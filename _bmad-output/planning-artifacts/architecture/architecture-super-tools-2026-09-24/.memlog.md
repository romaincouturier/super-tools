---
scope: super-tools entier (front React, Edge Functions, base Supabase), photo de l'existant
purpose: build-substrate : contrat court que bmad-build, bmad-loop et Lovable respectent
altitude: initiative
updated: 2026-09-24T16:20
---

- (constraint) Front: hooks appellent supabase en direct (145 hooks importent le client vs 28 services). components+pages: 156 importent le client. Regle [014] cible page->hook->service, realite majoritaire page->hook->supabase.
- (decision) AD candidat: la couche d'acces donnees ratifiee est le hook (src/hooks) ; les services sont optionnels pour logique partagee. Nouveau code: aucun acces supabase dans components/pages. Binds: tout le front. Prevents: 3e voie d'acces.
- (constraint) Routage: garde RequireStaff (profil existant) et RequireLearner via useSession().status {loading,anon,none,learner,staff} ; 13 routes publiques /:token sans garde ; routes publiques par id (/formation-info/:trainingId, /lms/:courseId/player...).
- (constraint) React Query: queryClient App.tsx:149 staleTime 5min, refetchOnWindowFocus false, erreurs -> reportHandledError (Sentry). Pas de store global. Query keys ad hoc (388 litteraux), pas de fichier central.
- (constraint) Mode demo: useDemoMode() + fonctions pures lib/demoMask.ts appliquees inline, 139 fichiers.
- (constraint) Organisation: seul components/ est par domaine (lms 105, formations 100, crm 33...). hooks (183 a plat), services (28 a plat), lib (140 a plat). Pas de src/modules.
- (constraint) Edge: 243 fonctions, toutes declarees config.toml, 228 verify_jwt=false (regle [063] garde applicative). Declencheurs: front (102 invoke + 21 fetch), publiques a token a usage unique, pg_cron (10 fonctions via net.http_post, 29 migrations cron.schedule), 7 webhooks, MCP (supabase/functions/mcp-server ~40 outils, OAuth mcp_oauth_records ; proxy Node mcp-server/ a la racine).
- (constraint) Auth edge: verifyAuth() _shared/supabase-client.ts:62 (72 fonctions) ; admin via rpc is_admin ; interne via isInternalCall()/isInternalOrAuthenticated() _shared/cron-auth.ts (x-cron-secret, service role) peu adopte (7 fonctions).
- (constraint) Edge reponses: _shared/cors.ts corsHeaders/handleCorsPreflightIfNeeded/createErrorResponse/createJsonResponse ; 119/243 createErrorResponse (ratchet [037b]). Client: getSupabaseClient() service role partage, 109 createClient directs.
- (constraint) IA: aiChat() _shared/ai.ts:171 multi-fournisseurs (lovable, anthropic, openai, gemini) ; ids _shared/claude-models.ts ; 15 fonctions appellent api.anthropic.com en direct ; cout via _shared/api-usage.ts (regle [045]).
- (constraint) Integrations: un module _shared par tiers pour Pennylane, Google, Resend, Signitic, BOAMP/TED, Slack ; sans module: Stripe(6), WordPress(3), Brevo(2), Fireflies(3), Gemini(3), GitHub(2), AssemblyAI(11). Regle [052] impose un exemplaire unique dans _shared.
- (decision) AD candidat: toute edge function classe son declencheur (user JWT / token public / interne cron / webhook signe / MCP) et applique la garde correspondante du _shared ; aucune ne repose sur verify_jwt seul. Binds: supabase/functions. Prevents: fonction sensible ouverte.
- (decision) AD candidat: dialogue avec un tiers = un seul module _shared/<tiers>.ts (ratifie [052]) ; nouveaux appels Stripe/WordPress/AssemblyAI... creent le module au premier usage. Prevents: copies divergentes.
- (decision) AD candidat: appel LLM passe par aiChat() (fournisseur, modele, log de cout) ; ids de modele seulement dans claude-models.ts. Prevents: modele code en dur, cout non trace.
