# Audit d'implémentation — Tickets ST-2026-0199 → ST-2026-0207

Date : 2026-06-30. Méthode : relecture du code et des migrations associés à chaque
ticket (commit cité), vérification correction / complétude / sécurité RLS / dépendances
de déploiement. La base de tickets support n'étant pas accessible depuis cet
environnement, le périmètre de chaque ticket est déduit du message de commit.

| Ticket | Commit | Verdict |
|--------|--------|---------|
| ST-2026-0199 — Commentaires + dépôt PDF sur la veille | `c687615` | Corrigé (faille d'upload comblée) |
| ST-2026-0200 — Ancienneté des commentaires (communauté) | `f128d3a` | OK |
| ST-2026-0201 — Centre de notifications in-app apprenant (live J-3, replay) | `5508b73` | OK |
| ST-2026-0202 — Correction libellé « par d'autres apprenants » | `9449097` | OK |
| ST-2026-0203 + 0205 — Dépôts de leçon : multi-fichiers, masquage pairs, confidentialité email | `3bcf121` | OK |
| ST-2026-0206 — Alerte admins instantanée + notif in-app sur commentaire | `7562aef` | OK |
| ST-2026-0204 | — | Introuvable dans le dépôt |
| ST-2026-0207 | — | Introuvable dans le dépôt |

## ST-2026-0199 — Veille : commentaires + dépôt PDF
- Champ `comment` persisté (migration `20260623100000`) et affiché (`WatchItemCard.tsx`), édition OK.
- Type `document` / `application/pdf` ajouté (migration `20260623100100`), filtre UI présent.
- Upload via edge function `upload-watch-file` (conforme règle [026d], pas de `storage.upload()` dans `src/`).
- FAILLE TROUVÉE ET CORRIGÉE : l'edge function `upload-watch-file` acceptait des uploads
  sans aucune authentification (client service_role, bucket public `watch`). Ajout de
  `verifyAuth()` — le module veille étant réservé au staff, un JWT valide est désormais exigé.
  Déploiement requis : `supabase functions deploy upload-watch-file`.

## ST-2026-0200 — Ancienneté des commentaires
- `formatDistanceToNow(new Date(c.created_at), { locale: fr, addSuffix: true })` correct et rendu dans l'en-tête de commentaire. RAS.

## ST-2026-0201 — Notifications in-app apprenant
- RLS `learner_notifications` : lecture/maj restreintes via `get_learner_email()`. Lecture côté client via `createLearnerClient` (header `x-learner-email`), correct.
- Live J-3 : cron `pg_cron` quotidien + `upsert(onConflict, ignoreDuplicates)` + index unique de dédup.
- Replay : trigger `notify_replay_available()` AFTER UPDATE de `replay_url` avec `ON CONFLICT DO NOTHING`. RAS.

## ST-2026-0202 — Libellé
- Chaîne corrigée en « Travaux partagés par d'autres apprenants ». Aucune autre occurrence fautive dans l'UI. RAS.

## ST-2026-0203 + 0205 — Dépôts de leçon
- Multi-fichiers (`MAX_DEPOSIT_FILES = 10`, input `multiple`, upload par fichier via edge function).
- Masquage des pairs par défaut + bascule persistée (localStorage).
- Confidentialité email : `depositAuthorLabel()` et `DepositCommentList` n'affichent jamais l'email brut (nom résolu ou `maskEmail`). Colonne `author_display_name` (migration `20260623102000`) cohérente. RAS.

## ST-2026-0206 — Alerte admins + notif in-app
- `notify-practice-comment` invoquée après insertion du commentaire (`usePracticeFeed.ts`), fire-and-forget.
- Email à tous les admins (`is_admin = true`), notif in-app `community_reply` utilisant le NOM du commentateur (pas l'email).
- Migration `20260623103000` ajoute `community_reply` au CHECK. RAS.

## ST-2026-0204 et ST-2026-0207
- Aucun commit, code, migration ou commentaire ne référence ces identifiants. Numéros sautés
  dans la séquence (0203, 0205, 0206, 0208 présents) ou tickets non implémentés. À vérifier
  côté kanban support : leur contenu n'est pas vérifiable depuis le dépôt.

## Action de déploiement
- `supabase functions deploy upload-watch-file` (correctif de sécurité ci-dessus).
- Rappel : les migrations des tickets 0199/0201/0206 doivent être appliquées en prod
  (`supabase db push`) si ce n'est pas déjà fait.
