# Tables volontairement exclues du backup (règle [038]).
# Une table listée ici n'est PAS exportée par backup-export / scheduled-backup.
# N'exclure que des données éphémères ou regénérables. Une ligne par table.
agent_embedding_cache
mcp_oauth_records
formulaire_rate_limits
indexation_queue
pictodico_rate_limit
backup_runs
# okr_scheduled_emails : table declaree en migration mais jamais creee en prod et
# inutilisee par le code (aucune reference hors backup). Son export faisait echouer
# chaque run (dbErrors > 0) et declenchait l alerte "aucune sauvegarde depuis 24h".
okr_scheduled_emails
# api_usage_events : télémétrie de consommation des APIs payantes, volumineuse et
# purgée à 180 jours. Aucune donnée métier, rien à restaurer.
api_usage_events
# vhd_report_narratives : récit nominatif d'un signalement de violence, de
# harcèlement ou de discrimination. Le registre (vhd_reports) est sauvegardé et
# prouve le traitement ; le témoignage lui-même ne quitte pas la base, pour ne
# pas en laisser de copie en clair dans Drive pendant toute la rotation GFS.
vhd_report_narratives
